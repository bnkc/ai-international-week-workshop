[project]
name = "ai-surviver-workshop"
version = "0.1.0"
description = "AI Survivor: Build an Agent in 2 Hours, Then Watch Them Scheme"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
]
