[project]
name = "ai-international-week-workshop"
version = "0.1.0"
description = "AI Vending Machine Workshop: Build an Agent in 2 Hours"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
    "requests>=2.32.5",
]
